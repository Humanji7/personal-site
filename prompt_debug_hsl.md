# 🎨 Debug: Плавный HSL-градиент для Sphere

## Контекст
Three.js сфера из 5000 частиц. При движении курсора цвет должен плавно переходить через множество микротонов: **фиолетовый → розовый → коралловый → ember**.

## Проблема
Сейчас переход **резкий** и видно только **2 цвета**. Причина: `THREE.Color.lerp()` интерполирует в RGB (прямая линия), а не в HSL (дуга по hue wheel).

## Что нужно исправить
В `prototype-sphere/src/ParticleSystem.js` функция `setColorProgress(progress)` (строка ~442):

```javascript
// СЕЙЧАС (плохо — RGB lerp)
resultColor = purple.clone().lerp(pink, t)

// НУЖНО (HSL lerp через hue wheel)
// purple H≈0.75 → ember H≈0.07 через 0/1 wrap
```

## Ожидаемый результат
При увеличении velocity/tensionTime цвет проходит через:
```
purple(270°) → violet → magenta → pink → salmon → coral → orange → ember(45°)
```

## Подсказка
В fragment shader уже есть рабочий `lerpHSL()` для evaporation — можно переиспользовать логику в JS через `THREE.Color.getHSL()` / `setHSL()`.

## Запуск
```bash
cd prototype-sphere && npm run dev
# Хаотичное движение курсора меняет цвет
```

## Критерии успеха
- [x] Градиент проходит через 4+ промежуточных оттенков
- [x] Переход визуально плавный, без скачков  
- [x] 60 FPS
- [x] Дыхание (сжатие-расширение) работает

## Бонус
- [x] Частицы evaporation улетают **выше** от сферы (drift увеличен с 15% до 50%)
