name = "postvoid-entitlements"
main = "src/index.js"
compatibility_date = "2026-01-01"

# Bindings (configure in Cloudflare dashboard or via `wrangler`):
# - KV namespace: ENTITLEMENTS_KV
# - Secrets: LEMON_WEBHOOK_SECRET, SESSION_TOKEN_SECRET
#
# Optional:
# - ALLOWED_ORIGIN (e.g. https://your-domain.com)

[vars]
ENV = "prod"

