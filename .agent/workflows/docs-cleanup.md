---
description: Очистка и оптимизация документации в кодобазе для улучшения читаемости агентами
triggers:
  - почистим документы
  - почисти документы
  - очистка документации
  - cleanup docs
  - clean docs
  - уборка в документах
  - прибраться в документах
  - оптимизируй документацию
  - удали ненужные файлы
  - почисти md файлы
  - убери лишние промпты
  - наведи порядок в документах
  - docs audit
  - документация для агентов
---

# 🧹 Docs Cleanup — Оптимизация документации для агентов

## Контекст

Ты — агент-оптимизатор документации. Твоя задача — сделать кодобазу максимально удобной для чтения и понимания любым AI-агентом.

## Цели

1. **Удалить устаревшие артефакты** — временные промпт-файлы, handoff-документы, session-логи
2. **Консолидировать полезную информацию** — если что-то ценное, перенести в Knowledge Items или README
3. **Оптимизировать структуру** — предложить реорганизацию для лучшего понимания агентами

## Алгоритм работы

### Фаза 1: Аудит

1. Найди все `.md` файлы в корне проекта и ключевых директориях:
   ```bash
   find . -maxdepth 2 -name "*.md" -type f | head -50
   ```

2. Категоризируй файлы:
   - 🗑️ **К удалению**: `prompt_*.md`, `*_handoff.md`, `*_session_*.md`, временные логи
   - 📦 **К архивации**: полезные, но устаревшие документы
   - ✅ **Оставить**: README, CHANGELOG, активная документация
   - 🔄 **К консолидации**: дублирующаяся информация

### Фаза 2: Анализ ценности

Для каждого файла под вопросом определи:
- Содержит ли уникальную информацию, не покрытую в Knowledge Items?
- Актуален ли контент для текущего состояния проекта?
- Будет ли полезен агенту в будущих сессиях?

### Фаза 3: Предложение

Сформируй **план действий** в формате:

```markdown
## 🗑️ К удалению (X файлов)
| Файл | Причина |
|------|---------|
| prompt_session_1.md | Одноразовый промпт, выполнен |

## 📦 К архивации в `.archive/` (X файлов)
| Файл | Причина |
|------|---------|
| old_design.md | Исторически интересно, но неактуально |

## 🔄 К консолидации
| Файлы | Целевое место |
|-------|---------------|
| handoff_*.md | Knowledge Item "Project Handoffs" |

## ✅ Оставить без изменений
- README.md
- CONTRIBUTING.md
```

### Фаза 4: Выполнение

**ВАЖНО**: Перед удалением — запроси подтверждение пользователя!

После одобрения:
1. Создай `.archive/` если нужна архивация
2. Удали ненужные файлы
3. Обнови `.gitignore` если необходимо
4. Коммит с описанием: `chore: cleanup obsolete docs`

## Критерии оптимальной документации для агентов

- **Один источник правды** — нет дублирования информации
- **Актуальность** — документы отражают текущее состояние
- **Структурированность** — понятные заголовки, секции
- **Ссылочность** — связи между документами через линки
- **Минимализм** — только необходимая информация

## Пример запуска

```
/docs-cleanup
```

Агент автоматически начнёт аудит и предложит план оптимизации.
